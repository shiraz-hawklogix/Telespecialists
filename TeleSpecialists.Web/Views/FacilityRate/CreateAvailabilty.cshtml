@using TeleSpecialists.BLL.Helpers;
@using TeleSpecialists.BLL.Extensions;
@using TeleSpecialists.BLL;
@model TeleSpecialists.BLL.Model.facility_availability_rate
@{
    ViewBag.Title = "Add Availability Rate";
    ViewBag.Edit = false;
}
<h4>Add Availability Rate</h4>
<hr />
@Html.Partial("_Availability", Model)

<script type="text/javascript">
    $(document).ready(function () {
        document.title = $("#title-prefix").val() + '@ViewBag.Title';
        //$("#rate_end_Date").kendoDatePicker({
        //    start: "year",
        //    depth: "year",
        //    format: "MMMM yyyy",
        //    open: function (e) {
        //        onDatePickerOpen(e);
        //    }
        //    });
        $("#rate_start_Date").kendoDatePicker({
            start: "year",
            depth: "year",
            format: "MMMM yyyy",
            open: function (e) {
                onDatePickerOpen(e);
            }
            });

        $('#Facilities').multiselect({
            columns: 1,
            placeholder: ' -- Select --',
            search: true,
            searchOptions: {
                'default': 'Search'
            },
            selectAll: true
        });
    });

</script>



