@using TeleSpecialists.BLL.Helpers;
@using TeleSpecialists.BLL.Extensions;
@using TeleSpecialists.BLL;
@model TeleSpecialists.BLL.Model.physician_percentage_rate
@{
    ViewBag.Title = "Update Physician Incentive Rate";
    ViewBag.Edit = true;
}
<h4>Update Physician Incentive Rate</h4>
<hr />

<div class="alert case-info">
    <div class="row">
        <div class="col-9">
            <div class="row">
                <div class="d-inline-block">
                    <strong>Physician:</strong> <span> @ViewBag.name </span>
                </div>
            </div>
        </div>
    </div>
</div>
@Html.Partial("_FormPercentage", Model)
<script type="text/javascript">
    $(document).ready(function () {
        var currentFacilityOffset = @(Functions.GetTimeZoneOffset(Settings.DefaultTimeZone));
        document.title = $("#title-prefix").val() + '@ViewBag.Title';
        $("#phy_list").fillKendoDropDown("/LookUp/GetAllPhysicians", "Name", "Id", "-- Select --");
        $("#rate_end_Date").kendoDatePicker();
        $("#rate_start_Date").kendoDatePicker();
    });
        $('.cls_shift_name').on('change', function () {
            $('#lbl_shift_name').val($("#shift_name_id option:selected").text());
        });
</script>



